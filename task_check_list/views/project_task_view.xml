<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="view_task_form2_inherit_form_view" model="ir.ui.view">
        <field name="name">project.task.form.view</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project.view_task_form2"/>
        <field name="arch" type="xml">
            <page name="extra_info" position="after">
                <page name="checklist" string="Checklist">
                    <group>
                        <group string="Checklist">
                            <field name="available_checklist_ids" invisible="1" />
                            <field name="task_checklist" widget="many2many_checkboxes" nolabel="1"
                                   domain="[('id', 'in', available_checklist_ids)]" />
                        </group>
                        <group string="Completed">
                            <field
                                name="checklist_progress"
                                widget="percentpie"
                                string="Progress"
                                style="--PercentPieField-size: 80px; --PercentPieField-gap: 12px;"
                            />
                        </group>
                    </group>
                </page>
            </page>
        </field>
    </record>

    <record id="view_task_tree2_inherit_tree_view" model="ir.ui.view">
        <field name="name">project.task.list.view</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project.view_task_tree2"/>
        <field name="arch" type="xml">
            <field name="stage_id" position="before">
                <field name="checklist_progress" widget="progressbar" />
            </field>
        </field>
    </record>
</odoo>

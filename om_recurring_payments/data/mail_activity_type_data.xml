<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- the payments the scheduled action could not create -->
        <record id="mail_activity_type_recurring_payment_failed" model="mail.activity.type">
            <field name="name">Recurring Payment Failed</field>
            <field name="summary">Payment failed</field>
            <field name="icon">error</field>
            <field name="decoration_type">danger</field>
            <field name="res_model">recurring.payment</field>
            <field name="delay_count">0</field>
            <field name="sequence">60</field>
        </record>

    </data>
</odoo>

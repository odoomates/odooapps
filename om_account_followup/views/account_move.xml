<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_move_line_reconcile_tree" model="ir.ui.view">
            <field name="name">account.move.line.list</field>
            <field name="model">account.move.line</field>
            <field name="arch" type="xml">
                <list string="Journal Items to Reconcile" create="false">
                    <field name="date"/>
                    <field name="move_id"/>
                    <field name="ref"/>
                    <field name="name"/>
                    <field name="partner_id"/>
                    <field name="account_id"/>
                    <field name="journal_id" column_invisible="1"/>
                    <field name="full_reconcile_id"/>
                    <field name="debit" sum="Total debit"/>
                    <field name="credit" sum="Total credit"/>
                </list>
            </field>
        </record>

        <record id="account_manual_reconcile_action" model="ir.actions.act_window">
            <field name="context">{'search_default_unreconciled': 1,'view_mode':True}</field>
            <field name="name">Journal Items to Reconcile</field>
            <field name="res_model">account.move.line</field>
            <field name="view_id" ref="view_move_line_reconcile_tree"/>
            <field name="view_mode">list</field>
            <field name="help" type="html">
                <p>No journal items found.</p>
            </field>
        </record>


        <record id="account_move_line_partner_tree" model="ir.ui.view">
            <field name="name">account.move.line.partner.list</field>
            <field name="model">account.move.line</field>
            <field eval="32" name="priority"/>
            <field name="arch" type="xml">
                <list editable="bottom" string="Partner Entries">
                    <field name="date"/>
                    <field name="move_id"/>
                    <field name="ref"/>
                    <field name="name"/>
                    <field name="partner_id"/>
                    <field name="company_id" column_invisible="1"/>
                    <field name="company_id" groups="base.group_multi_company" readonly="1"/>
                    <field name="account_id" options="{'no_open': True, 'no_create': True}"
                           domain="[('company_ids', 'in', company_id)]" groups="account.group_account_readonly"/>
                    <field name="followup_line_id"/>
                    <field name="followup_date"/>
                    <field name="debit" sum="Total debit"/>
                    <field name="credit" sum="Total credit"/>
                    <field name="date_maturity"/>
                </list>
            </field>
        </record>

        <record id="view_move_line_form" model="ir.ui.view">
            <field name="name">account.move.line.form.followup</field>
            <field name="model">account.move.line</field>
            <field name="inherit_id" ref="account.view_move_line_form"/>
            <field name="arch" type="xml">
                <field name="date_maturity" position="after">
                    <field name="followup_line_id"/>
                    <field name="followup_date"/>
                </field>
            </field>
        </record>

        <!-- the open items listed from the follow-ups -->
        <record id="view_followup_open_items" model="ir.ui.view">
            <field name="name">account.move.line.followup.open.items</field>
            <field name="model">account.move.line</field>
            <field name="priority">40</field>
            <field name="arch" type="xml">
                <list string="Open Items" create="false" delete="false" edit="false"
                      decoration-muted="followup_disputed"
                      decoration-danger="not followup_disputed and date_maturity and date_maturity &lt; context_today().strftime('%Y-%m-%d')">
                    <field name="date"/>
                    <field name="move_id"/>
                    <field name="ref" optional="show"/>
                    <field name="date_maturity"/>
                    <field name="followup_line_id" optional="show"/>
                    <field name="followup_date" optional="show"/>
                    <field name="followup_disputed"/>
                    <field name="company_currency_id" column_invisible="1"/>
                    <field name="balance" string="Amount" sum="Total"/>
                    <field name="amount_residual" string="Open Amount" sum="Total"/>
                </list>
            </field>
        </record>

        <!-- a disputed invoice is left out of the reminders -->
        <record id="view_move_form_followup" model="ir.ui.view">
            <field name="name">account.move.form.followup</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <xpath expr="//group[@name='sale_info_group']" position="inside">
                    <field name="followup_disputed" invisible="move_type != 'out_invoice'"/>
                    <field name="followup_dispute_reason"
                           invisible="move_type != 'out_invoice' or not followup_disputed"/>
                </xpath>
                <xpath expr="//div[@name='button_box']" position="after">
                    <div class="alert alert-warning mb-0" role="alert" invisible="not followup_disputed">
                        The customer disputes this invoice: it is left out of the payment reminders.
                        <field name="followup_dispute_reason" class="oe_inline" readonly="1"
                               invisible="not followup_dispute_reason"/>
                    </div>
                </xpath>
            </field>
        </record>

        <record id="view_account_invoice_filter_followup" model="ir.ui.view">
            <field name="name">account.invoice.select.followup</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_account_invoice_filter"/>
            <field name="arch" type="xml">
                <xpath expr="//search" position="inside">
                    <filter string="Disputed" name="followup_disputed" domain="[('followup_disputed', '=', True)]"/>
                </xpath>
            </field>
        </record>

    </data>
</odoo>

<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.currency.revaluation</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="account.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//block[@name='main_currency_setting_container']" position="inside">
                <setting id="om_currency_revaluation" string="Currency Revaluation" company_dependent="1"
                         groups="account.group_account_manager"
                         help="Restate the open foreign currency balances at the rate of a chosen date: the
                               difference is posted on these accounts as an unrealized gain or loss.">
                    <div class="content-group">
                        <div class="row mt8">
                            <label for="currency_reval_journal_id" class="col-lg-4 o_light_label"/>
                            <field name="currency_reval_journal_id"/>
                        </div>
                        <div class="row mt8">
                            <label for="currency_reval_gain_account_id" class="col-lg-4 o_light_label"/>
                            <field name="currency_reval_gain_account_id"/>
                        </div>
                        <div class="row mt8">
                            <label for="currency_reval_loss_account_id" class="col-lg-4 o_light_label"/>
                            <field name="currency_reval_loss_account_id"/>
                        </div>
                        <div class="mt8">
                            <field name="currency_reval_reverse" class="oe_inline"/>
                            <label for="currency_reval_reverse" class="o_light_label"
                                   string="Reverse the adjustment at the start of the next period"/>
                        </div>
                    </div>
                </setting>
            </xpath>
        </field>
    </record>

</odoo>

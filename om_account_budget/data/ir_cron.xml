<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="ir_cron_budget_warnings" model="ir.cron">
        <field name="name">Budget: warn the responsibles of over budget lines</field>
        <field name="model_id" ref="model_account_budget"/>
        <field name="state">code</field>
        <field name="code">model._cron_warn_budget_responsibles()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
    </record>

</odoo>
